# TECH SPEC

Canonical source: [`.spec/techspec.md`](.spec/techspec.md).

This file is a pointer so the repo root advertises the tech spec. The authoritative content lives under `.spec/` and should be edited there. v0.1 locks in: Python ≥3.11 / <3.14, CustomTkinter GUI, local rule-based generator (no AI API), no database.
