# Road v0.8 施工计划：从 Mac 本地创作到双端同步

> 2026-09-07 重规划草案。当前源码基线为 `5ff26fe`：保留计划重构及已验收关闭保护。
> 之前 CP0–CP4 尝试已回滚，历史分支仅供定位经验，不算本轮实现或通过证据。
> 开发者已授权本轮 CP0–CP5 工程、提前 YOLO 和逐阶段本地提交；在独立 worktree 实施，不恢复旧任务。

## 0. 先看我们正在改什么

打开[双端改动图](docs/architecture/road-v0-8-changes.html)，从上到下看：
上排是保留的 Mac 本地链；下排是准备增加的 iPhone、Rust Core 与 iCloud 链。
图中 CP 编号与本计划一致；[可编辑图规格](docs/architecture/road-v0-8-changes.archify.json)随计划维护。

| 看得见的变化 | 现在：`5ff26fe` | 本 Road 结束 | 改动位置／阶段 |
| --- | --- | --- | --- |
| Mac 原有工作台 | 本地创作、资料管理可用 | 完整保留 | `frontend/src/App.vue`、现有 Python 链；回归保护 |
| 文件规则 | Python 处理 Paper v4 | 移动与云端使用同一 Rust 实现 | `frontend/src-tauri/src/` 新增 Paper 模块；CP2 |
| iPhone 写作 | 本轮基线无产品入口 | 多页编辑、搜索、导出、恢复 | Vue 移动入口及原生宿主；CP3 |
| 两端打开同一篇稿 | 没有产品同步入口 | 主动选择 iCloud，同步 Paper | Apple 文件适配与存储选择；CP1、CP4 |
| 离线或同时写稿 | 尚无跨设备处理 | 明确显示未就绪、冲突及恢复版本 | 发现、下载、保全、只读对账；CP4 |
| 交给你测试 | 只有 Mac 本地基线 | 同一源码的两个安装包与一份测试单 | CP5 |

**当前游标：CP2 共享 Rust Paper Core。** 每次交付只更新下面这一行，不能用“代码已写”冒充设备验收。

| 本轮状态 | 当前修改 | 已确认 | 下一可见成果 | 待人工 |
| --- | --- | --- | --- | --- |
| 机器检查通过 | [CP3 本机创作与恢复](docs/acceptance/road-v0-8/cp3-local.md) | 原文恢复保护、双语、多页、系统导出实现及实体宿主保存／重启 | CP4 两端 iCloud 工程 | UIKit 输入与系统面板等批次 B 尚未执行 |

施工过程中沿用此表，状态只用“未开始／实现中／机器检查通过／待设备证据／已验收”。
进度报告说明当前 CP、具体改动、实际检查及下一步；不用测试数量代替用户能看到的成果。

## 1. 状态与目标

### 1.1 交付目标

保留 Vue/Tauri，以降低长期维护成本为目标逐步统一 Rust Core。Road v0.8 交付实体 iPhone
核心创作与 Mac 同步候选；iCloud 往返是必需验收项，不能以本地版本替代。

```text
关闭保护已验收 → Road v0.8：iPhone 核心创作与 Mac 同步候选
                  → 独立统一核心收口：桌面对等、Python 产品运行时退役
                  → Road v0.9：iOS/macOS 首轮外部 Alpha 与正式宣发 Gate
                  → Road v0.10：Android 与二轮 Alpha → 再决定 Windows
```

产品边界以 [SPEC](docs/SPEC.md) 为准，当前坐标以 [PROJECT](docs/PROJECT.md) 为准，
Git、数据安全和证据纪律引用 [RULES](docs/RULES.md)，不在各阶段重复建立权限规则。
架构选择及失效条件见 [ADR-0009](docs/architecture/decisions/0009-unified-rust-core-transition.md)。
旧 Road／ADR 中当时的未来排期保留为历史记录；当前施工和分发顺序以本计划及 SPEC 为准。

### 1.2 范围

- iPhone：多页 Paper、中文/英文、创建编辑、整体保存、重开、列表搜索、单篇 Markdown 导出、
  本机草稿恢复，以及与 Mac 的共享 iCloud Documents Vault 往返。
- macOS 本地：保留完整现有 Library、Index、Trash、文件夹、历史迁移、恢复与外部编辑器能力。
- macOS 云端与 iPhone：使用同一 Rust Paper Core；v0.8 云端只提供上述核心创作能力。
- 不包含 iPhone 完整资料管理、历史迁移、外部编辑器、批量导出、整库 ZIP、正式 iPad 验收；
  Android、Windows、Intel Mac、Linux、watchOS 不进入本 Road。
- 不新增 AI 代写、数据库、账号、自建同步、遥测、后台服务、localhost、通用插件或存储框架。
- 外部 TestFlight、Developer ID 最终候选、公证 DMG、招募和正式宣发属于 Road v0.9。

### 1.3 前置条件与批准状态

关闭保护是独立前置修复：2026-09-05 的候选已将监听器移至 App，并完成 Paper、Library、
Vault、阻塞页面及菜单退出的聚焦／原生检查；[修复报告](docs/acceptance/close-guard-2026-09-04.md)
保留代理原生检查范围与测试工具事件。开发者于同日人工复核确认：除强制退出外，所有常规退出入口受到保护；独立关闭保护前置验收通过。此结论不代表 CP0 启动。

本轮从 `5ff26fe` 重新开始。旧尝试暴露的问题作为设计输入：启动失败必须可见；原生版本
标识不能假定支持安全归档；系统版本可能只有文件读取权限；同 code 新建可能得到 provider
改名副本而非 NSFileVersion 冲突。每项都要在本轮实现和测试中重新证明，不整包恢复旧分支。

2026-09-07 开发者已批准新版并明确开始 CP0–CP5 工程，授权隔离工作区及逐阶段本地提交。已授权范围内的检查、修复和构建连续执行；
阶段是否通过由本计划的证据决定，不逐按钮请求确认。本轮仍不授权真实 Vault 迁移或外部发布。

## 2. 架构与过渡

### 2.1 当前、候选与最终目标

| 节点／环境 | 产品执行后端 | 能力与退出边界 |
| --- | --- | --- |
| 当前 macOS 本地 | JSONL protocol v2 + Python service/core | 唯一已接受运行链，保持现有合同 |
| v0.8 macOS 本地 | 现有 Python sidecar | 保留全部已接受能力，暂不切换后端 |
| v0.8 iPhone 本地 | 进程内 Rust Paper Core | 沙盒内核心创作、恢复和导出 |
| v0.8 Mac/iPhone 云端 | 同一 Rust Core + Apple 原生文件协调 | 核心创作、往返同步与冲突恢复 |
| 候选后的独立收口 | Rust 接管桌面剩余产品能力 | 对等验收与切换验证后移除 sidecar、产品 JSONL 调用和 Python 打包依赖 |
| 首轮外部 Alpha | 已完成统一核心收口的候选 | 不携带双后端；Python 仅可保留为开发期对照工具 |

选择 Rust 是维护策略，不是“iOS 无法运行 Python”的平台事实。当前子进程不能直接搬到
Tauri iOS；Python 官方支持嵌入解释器，但本路线不增加移动解释器打包与原生调用链。
Vue 保留可见状态和草稿，Core 保留产品规则；Apple 适配层只负责平台文件与生命周期能力。

### 2.2 接口与唯一写入者

- 保留 `bridgeRequest(method, params)` 前端边界和共享业务语义；桌面 Python 的 JSONL
  protocol v2 在过渡期冻结，host 新增能力不自动扩展该协议或修改其严格 DTO。
- CP0 分别冻结共享 Paper DTO、平台／存储能力声明、host 专属方法、错误分类和写入所有者。
  明确能力声明的来源与时机，不能直接把新字段塞入既有 `system.hello` 或 `startup.load`。
- 移动与云端 Library 可扫描受支持的根目录／一层文件夹并线性搜索，不复制 Index v4。
  CP0 明确无 Index 的结果投影与 UI 行为，不能伪造 `index_state=current`。
- 新增存储选择、单篇导出、恢复与冲突方法分别分类；保存未知结果沿用只读对账语义。
  未分类方法不得启用，旧请求结果不得应用于新的存储身份或草稿修订。
- 每个客户端对所选 Vault 只启用一个受控写入后端；这不假设跨设备只有一个写入者。
  禁止 Python/Rust 双写和失败后的自动后端回退；跨设备并发由文件协调与冲突合同处理。

## 3. 数据与交互合同

### 3.1 Paper、保存和导出

Paper v4 的权威语法继续引用 [Paper v4 设计 §8](docs/design/road-v0-6-paper-v4-design.md#8-markdown-schema-v4)。
迁移语言不升级格式、不批量改写现有作者文件。未知 frontmatter 保留解码后的键值与顺序，
不扩大为任意原始空白和字节布局保真承诺；明确保存仍遵循既有规范序列化。

- Python 产生经审阅的合法／非法／边界样本，两端消费同一版本化 golden corpus；若源码与
  书面合同冲突，先裁定差异，不把 Python 输出自动视为规范。
- 覆盖 marker 转义、Unicode、空可选值、重复键／Summary、无效页面、路径与符号链接逃逸、
  code 冲突、首次／再次保存；格式错误进入 `repair_required`，不得部分打开或自动修复。
- 保存对打开快照做 CAS，同目录临时文件与安全替换不得暴露部分 Paper。分别验证替换前失败、
  已保存但响应丢失、外部修改；保留草稿，通过只读对账确认状态，不自动重发 mutation。
- 单篇导出经系统 Share Sheet／Files 明确交付 Markdown 副本；取消或失败不改变正式 Paper。

### 3.2 双语、输入与本机恢复稿

- 使用项目内 `zh-CN`／`en` 字符串表与现有 Vue 状态；首次按系统语言选择，不能匹配时用英文。
  设置可手动切换并保存到本机状态；错误、确认、恢复、导出、同步状态与辅助功能名称同样覆盖。
  作者正文、Tags、Paper 名与路径不翻译、不规范化，不引入通用 i18n 依赖。
- 复核 `375×812`、`720×900`、`720×680`、`920×680`、`1220×780` 五个已接受尺寸；
  在实体 iPhone 检查 safe area、软键盘、中文／英文 IME、
  光标／选区分页与前后台切换。不得用浏览器布局测试替代原生输入验证。
- 恢复稿按存储身份、Paper／未保存草稿身份和草稿修订区分；输入静止后及进入后台时尝试持久化。
  CP0 冻结触发时机、持久化确认、清理条件与本机存储／备份边界；恢复稿不进入 Vault 或同步。
- 只有对应修订明确保存成功或用户明确丢弃时才可清除；保存期间的新修订必须保留。
  恢复区写入失败必须可见，失败、`stale`、`repair_required`、`commit_unknown` 均不得清稿。
- 重启只提供明确恢复、导出、丢弃；不覆盖正式 Paper。终止测试只承诺恢复最后一次成功持久化
  的草稿，不承诺任意时刻强制终止都零丢字。

### 3.3 存储选择与 iCloud

- 默认本地；作者主动选择后，才创建或重新连接同一 Apple Account 的单个 App-owned 云端 Vault。
  不自动迁移已有本地 Vault；macOS 本地完整管理能力不得泄漏到云端窄能力界面。
- 切换前处理未保存草稿与未决写入。新目标验证成功后才更新选择；失败保留原选择，旧请求
  不得污染新 Vault。容器不可用、账号变化、文件未下载、协调失败均显示明确状态，不静默降级。
- 使用 Apple 原生 metadata discovery、按需下载、`NSFileCoordinator`、`NSFileVersion`
  或等价原生能力；普通目录扫描不能证明云端同步完成。
- 系统 current version 是活动版本候选，不按客户端时钟挑赢家。先保存并验证每个 losing
  version 的原始字节，再标记冲突已处理；未完成保全就不得宣称冲突已解决。
- 恢复副本具有不含设备／账号标识的唯一身份，记录可恢复状态；保存失败或中断后不能覆盖
  已有副本。“提升为当前版本”先保全当时活动版本，再做安全 CAS，不自动合并或删除版本。
- 同 code 离线新建同时覆盖原生冲突和 provider 改名副本：发现、显示、完整保全、导出与明确恢复。
  文件名与内嵌 code 不一致时不自动改名、改 code 或合并；明确选择后可从保全副本恢复，
  恢复前保全活动稿。具体恢复入口在 CP0 冻结。损坏版本仍能按原始字节导出，不能作为
  部分有效 Paper 编辑；用户可明确导出后另建 Paper。

## 4. 施工顺序：每一步都有可见结果

保留 CP0–CP5 编号，缩小每步交付物。工程实现、机器检查和设备验收分开记录。
Git 仍依 RULES §7：从符合授权及前置条件的 checkpoint 建分支；不因等待回复开一串候选分支。

| 阶段 | 改哪里 | 完成后能看到什么 | 自动检查重点 | 人工参与 |
| --- | --- | --- | --- | --- |
| CP0：定边界 | 共享 DTO、能力、写入与错误合同 | 一张方法责任表，知道谁读写什么 | 方法分类、文档链接、合同互斥 | 随本轮工程范围一次确认；不逐条追问 |
| CP1：接通 Apple | Tauri 移动入口与最小原生适配 | 同一测试文件在两个探针中可读 | 构建、签名核验、协调与原字节回读 | 设备批次 A：账号、信任、真机与真实容器 |
| CP2：做共享 Core | 现有 Rust 工程中的 Paper 模块 | 同一组 Paper 样本两端规则一致 | 解析、CAS、失败写入与路径防护 | 通常无需人工 |
| CP3：做手机本机创作 | Vue 移动入口、恢复区、系统导出 | 手机可从创建走到保存重开与恢复 | 编辑状态、修订清理、导出取消、启动失败 | 原生 smoke 能自动就自动；IME 与体验集中到 B |
| CP4：做双端同步 | 云发现、下载、协调、恢复界面 | 两端往返；两种冲突入口都能保全 | 未知结果、保全、切换、过期观察 | 真 provider 场景集中到 B，不逐操作等待回复 |
| CP5：交付测试候选 | 同源码双端包、证据与剩余问题 | 一份可照做的个人真机测试单 | 完整回归、包身份及源码一致性 | 设备批次 B 与最终候选接受 |

### CP0：合同冻结

- **输入条件：** 基线明确、关闭保护已接受、开发者明确启动新版工程。
- **交付物：** 最小方法责任表：调用方、读写所有者、能力、成功结果、失败与未知结果。
  复用 Paper v4 和 protocol v2；只定义移动／云端必要增量，不造通用存储框架。
- **检查：** 每个写操作能追踪到唯一所有者；无 Index 的端不伪造正常 Index 状态；文档检查通过。
- **退出条件：** 当前路线必需合同完整，未决项有明确答案；按工程授权记录阶段决定。
- **失败停点：** 只暂停依赖未决合同的实现；继续不依赖它的样本、界面和构建准备。

### CP1：Apple 可行性验证

- **输入条件：** CP0 合同完成，已有 Apple 配置与测试设备授权可用。
- **交付物：** 最小探针和可复用原生薄层；不维护第二套长期产品界面。冻结实际工具链与脱敏签名状态。
- **检查：** Rust 调用、沙盒、生命周期、原生协调、共享容器往返及原字节保全。
  合成文件只在专用测试区。普通目录扫描和构建成功不替代 provider 证据。
- **退出条件：** 平台路线成立，设备批次 A 的必要证据齐全，无已知根本阻断。
- **失败停点：** 真机／账号不可用时集中报告一次，等待外部状态改变；可继续 Core 样本等独立工作。
  原生安全文件能力不成立则停止依赖实现，回到 ADR-0009；不自动换栈或取消同步。

### CP2：共享 Rust Paper Core

- **输入条件：** CP0、CP1 的阶段前置条件满足。
- **交付物：** 严格解析／序列化、创建／打开、CAS 保存、列表／线性搜索；一套审阅过的共享样本。
- **检查：** §3.1 合法、损坏、Unicode、路径逃逸、同 code 与失败写入；Mac 旧路径回归。
- **退出条件：** 合同一致、安全失败不改作者文件；机器检查通过并记录工程阶段结果。
- **失败停点：** 不一致先修共享原因；不能靠普通覆盖、格式升级或双写绕过。

### CP3：iPhone 本机创作

- **输入条件：** CP2 工程结果满足合同及安全检查。
- **交付物：** 一个可见启动状态、一个创作循环、一个恢复入口；复用 Vue 与系统导出面板。
  失败必须显示可理解原因，不用空白页或闪退代替错误状态。
- **检查：** 创建、多页编辑、保存重开、搜索、导出／取消、后台持久化、失败保稿与修订清理。
  原生启动、恢复和输入证据分别记录；能自动执行的检查由代理完成。
- **退出条件：** 工程检查及可执行的原生 smoke 通过，可交付本机候选；IME／手感等未验项列入 B。
  工程 checkpoint 通过不宣称手机产品已人工验收。
- **失败停点：** 崩溃、丢稿、清错修订是工程阻断，先修；等待体验反馈不阻止独立云端工程工作。

### CP4：两端 iCloud

- **输入条件：** 本机保存与恢复安全链成立；CP1 配置对本轮仍有效。
- **交付物：** 明确的本地／云端选择、下载状态、双端读写和恢复界面；不新增账号或自建同步服务。
- **检查：** 在线往返、离线创建／编辑重连、并发保存、同 code 新建、多个冲突、中断与外部修改；
  另测账号／容器不可用、Drive 关闭、metadata-only、切换前后未决写入。
  **同 code 覆盖两种结果：原生版本冲突、provider 自动改名文件。** 不能只检查 NSFileVersion 数量。
- **退出条件：** 工程自动检查通过、真实 provider 证据逐项登记；能发现并保全全部版本、导出及明确恢复。
  工程可交付与 provider 验收分开；缺项进入 B 的必测清单，不能标为同步通过。
- **失败停点：** 字节无法保全、未知结果重写、旧请求污染新存储时停止危险操作；继续安全诊断和独立修复。

### CP5：跨端候选与个人真机测试

- **输入条件：** CP1–CP4 工程结果已记录，同步没有已知数据安全阻断；设备未验项有完整清单。
- **交付物：** 同一源码版本的 Mac/iPhone 开发候选、测试说明、版本身份、证据表、P2/P3 和回退办法。
  第一轮个人真机测试只用合成稿；真实作者 Vault 和最终分发另按明确授权执行。
- **检查：** Mac 完整本地回归、手机本机循环、双语／IME、恢复／切换、双端同步、冲突与导出；
  设备批次 B 补齐 CP3/CP4 真机及真实 provider 证据，记录实际候选，不复制旧分支通过次数。
- **退出条件：** 必需设备／provider 场景通过，无未解决 P0/P1，开发者明确接受 v0.8。
  “个人测试包已交付”是中间状态，不能写成“CP5 已通过”。
- **失败停点：** 缺设备证据就停在候选待验；不启动统一核心收口、外部 Alpha 或发布。

### 4.1 两批人工参与，工程检查连续执行

| 批次 | 代理提前准备 | 一次需要你做的事 | 留下什么证据 |
| --- | --- | --- | --- |
| A：平台接通 | 可安装探针、最小合成数据、已核验构建与配置 | 必需的登录／信任／解锁；确认真机可运行与容器可达 | 实际设备和 provider 结果；缺项直接标明 |
| B：个人真机测试 | 双端候选、每步预期与失败分支、备份及记录表 | 按一份连续脚本检查输入、前后台、离线重连与恢复 | 每项实际内容／副本／状态；最后统一接受或反馈 |

- 不把登录、系统信任或设备解锁伪装为可自动完成。只报告明确阻断及最短操作；不重复询问已有授权。
- 等待设备操作时继续样本、测试、构建、文档等独立工作；没有新状态不反复发“继续／仍等待”。
- 断网与重连这类有依赖的步骤仍按顺序，但通过批次脚本给出完整指引；只有异常才插入诊断。
- 每项记录“源码／环境／操作／预期／实得／副本校验／结论”。“没有问题”只说明用户观察正常，
  不能自动证明两份冲突字节都存在；代理能读回核对的部分自行完成，不再让用户重复核对。
- 人工批次是减少打断的默认安排，不是隐藏真实限制：发现需要新的系统操作，合并说明原因；
  紧急数据安全问题立即停写。不能为了减少问答删掉必要验收。

## 5. 验收与统一核心收口

### 5.1 检查与证据边界

文档阶段只运行文档检查与差异检查，并明确报告应用测试未运行：

```bash
.venv/bin/python scripts/check_docs.py
git diff --check
```

工程 CP 按改动面运行相关检查；共享数据／宿主变更及 CP5 执行完整回归。以下是命令入口，不是每次小改动都重跑的清单：

```bash
.venv/bin/python -m pytest
.venv/bin/python -m compileall -q src
npm --prefix frontend run test
npm --prefix frontend run build
cargo test --manifest-path frontend/src-tauri/Cargo.toml
cargo fmt --manifest-path frontend/src-tauri/Cargo.toml --check
.venv/bin/python scripts/check_docs.py
git diff --check
```

实际构建与设备命令由 CP0／CP1 按获准工具链冻结，优先复用现有命令。每条证据记录源码版本、命令、日期、结果
和遗漏；自动测试、模拟器、实体设备、真实 provider、作者验收、最终分发分别标记。
合成数据是默认边界，真实 Vault 不作故障实验。证据不记录正文、凭据、账号、设备身份或私有路径。

开发构建同步成功不证明最终分发可用。Road v0.9 必须使用最终 TestFlight iOS 和最终
Developer ID／公证／stapled macOS 候选重证共享容器；DMG 无法通过时按既有决定改用
macOS TestFlight，并对该实际候选重证，不能把渠道切换视为自动通过。

### 5.2 候选后的独立统一核心收口

该阶段位于 v0.8 候选验收之后、首轮外部 Alpha 之前，独立立项与批准，不能藏入 CP5。
目标及强制退出条件现在确定，具体 checkpoint 和分支在候选交接时根据剩余调用清单形成可审阅计划。

1. 盘点桌面所有剩余服务与调用方，把 Index、文件夹、移动／分支、Trash／永久删除、历史迁移、
   恢复、设备状态和系统动作逐项绑定现有行为及验收证据，不能以移动端不需要为由删除。
2. 在 fixtures／完整副本上按能力组迁入 Rust；保留路径约束、未知字段、冲突、备份与安全失败
   行为，历史迁移语义单独验证；不借语言迁移批量改写作者文件或引入第二种格式。
3. 验证完整桌面对等与候选切换。每个客户端单一后端写入，不双写；失败停留在可诊断状态，
   不自动重放操作或切回另一后端继续写入。真实 Vault 切换需要单独的明确授权。
4. 对等验收与切换验证通过后，移除 sidecar、产品 JSONL 调用和 Python 打包依赖；复跑完整
   桌面／iPhone／iCloud 回归，证明分发候选不再依赖 Python。Python 可仅作为开发期对照工具。

**收口 Gate：** 全部现有桌面能力对等、安全切换、无 Python 产品运行依赖、无未解决 P0/P1，
开发者明确接受。任一项未通过，首轮外部 Alpha 不启动；v0.8 通过不解除此门槛。
ADR-0004 在当前运行时仍有效，只有已接受的替换阶段才改变相应所有权，最终由收口 Gate 完成替代。

### 5.3 风险与停止条件

| 风险 | 控制与停止条件 |
| --- | --- |
| 双实现漂移或永久滞留 | 共享审阅样本，书面合同裁定差异；双后端不进入首轮外部 Alpha |
| 同步与分发能力过晚暴露 | CP1 前置原生验证；最终分发候选仍独立重证 |
| 恢复稿或冲突副本丢失 | 验证修订身份、保全字节和中断恢复；失败可见且阻断危险继续 |
| 桌面功能在统一时被删减 | 收口盘点每个现有能力，完成对等与切换验证后才退役 Python |
| 阶段与授权再次混杂 | CP0 只冻结合同，CP1 才做探针；未执行的必需动作标记阻塞 |

## 6. 后续决策记录

以下保留既有 Road v0.9／v0.10 决定，不作为 v0.8 的验收条件，也不授权现在执行。
[用户研究手册](docs/manual/prospect/alpha-audience-research.md)提供研究说明，不成为执行权威。

- 统一核心收口通过后，Road v0.9 才开展 iOS external TestFlight 与 macOS 分发验证及首轮 Alpha。
- 小范围招募仅用小红书与 X；报名 14 天，目标 60 份合格样本（中英各 30）；不足 40 时只延长
  一次 7 天，仍不足只报告探索性结果。18+ 自我声明，不采集生日或身份证明。
- 邀请 24 人，中英各 12；至少 8 人覆盖 iPhone、8 人覆盖 macOS，可双端重叠。
- 正式宣发 Gate：无未解决 P0/P1、无静默丢稿；至少 18 人完成且中英各至少 8；创建、编辑、
  保存、重开、搜索、导出六项逐项成功率至少 90%，分母为所有完成测试且被要求执行该任务者，
  未完成该任务计失败；至少 6 人完成 iPhone↔macOS iCloud 往返。
- Gate 通过后，小红书与 X 为主宣发，Bilibili 与 YouTube 为副宣发；微博、Reddit 只作研究观察。
- 正式宣发后 Road v0.10 开发 Android 并进行二轮 Alpha；复用 Rust Core，首版本地 Markdown
  与显式导入／导出，不新增跨平台云服务。Windows 待两轮证据后决定，Linux／watchOS 未排期。

## 7. 本版文档与图的校验

本次只改文档，未运行应用测试、构建或设备验收。文档检查与 `git diff --check` 通过。
Archify architecture 的 validate／deliver 为 showcase 9/9，0 错误、0 警告。
浏览器 visual-check 因本机图形进程启动失败未完成；不宣称四个桌面尺寸或视觉检查通过。

- 图规格 SHA-256：`5ef149b3ac35de6d9de1f03b738d88eafcdfd005c11d91f7f18d83d253545d74`。
- HTML SHA-256：`3d6dd27e2aea7e18502456be72efafc651de4d11fae6beed868a37fedb40bb83`。

## 8. 本轮工程记录

- CP0（2026-09-07）：[宿主合同](docs/design/road-v0-8-host-contract.md)冻结，逐项核对前端入口、Rust 分派、Python 白名单与 DTO；每个新增写操作均有所有者及未知结果处理。
- `scripts/check_docs.py` 与 `git diff --check` 通过；CP0 为文档合同阶段，未运行应用测试、构建或设备验收。提前 YOLO 覆盖 CP0 工程退出决定，下一阶段为 CP1。
- CP0 提交：`e84d0c5`。CP1：[本轮 Apple 平台证据](docs/acceptance/road-v0-8/cp1-apple.md)记录实际双端往返、保全、终止重开和构建兼容修复；平台可行性通过，提前 YOLO 覆盖工程退出，允许进入 CP2。
- CP1 提交：`69fb2f2`。CP2：[共享 Core 证据](docs/acceptance/road-v0-8/cp2-core.md)记录两端 corpus、文件安全、Unicode 选择、只读对账及本轮检查；提前 YOLO 覆盖工程退出，允许进入 CP3。

- CP2 提交：`50707f5`。CP3：[本机创作证据](docs/acceptance/road-v0-8/cp3-local.md)记录宿主分派、原文保护、双语编辑、系统导出、Scene 启动修复、设备宿主重启恢复与五尺寸检查。提前 YOLO 覆盖工程退出；可进入 CP4，明确保留批次 B 的原生界面与输入缺项。

### CP4 当前执行记录

两端存储路由、云发现／下载、协调读写、冲突原字节保全与明确恢复已实现；
本轮 Mac → iPhone → Mac 合成内容往返通过。详见 [CP4 证据](docs/acceptance/road-v0-8/cp4-cloud.md)。
工程检查通过，按提前 YOLO 收口本地 checkpoint 并进入 CP5 候选准备。完整 provider 场景和原生界面仍属于 CP5 批次 B，尚未宣称同步验收通过。

### CP5 当前执行记录

CP4 工程提交为 `ee7d488`。CP5 已交付同源码开发候选与 [个人真机测试单](docs/acceptance/road-v0-8/cp5-candidate.md)。
交付检查补充隔离 Mac 候选的 Sidecar 配置，避免普通启动继承原全局 Vault。
Mac 已完成合成稿两页创建／保存／重开、关闭取消保护及外部修改防覆盖检查；修复 Library 对全文结果的重复过滤。
前端全量 140 项通过，两端候选已重建；手机两页保存重开、中文重启保持及普通界面在线云稿往返已取证；离线、并发等 provider 批次 B 仍未齐；当前停留在候选已交付／待验，不标记 CP5 或 Goal 完成。

离线测试报告保存后 `validation_failed`；重连实际读回离线正文。已修复成功保存后清空原文快照导致再次保存缺失恢复记录的问题，
前端 141 项通过，双端新候选在线连续保存通过。原始错误、保全副本和时间证据已记录；新版离线编辑复测正常，新增韩文字段已在 Mac 重连读回。Mac 未闭合 Tags 两页草稿关闭重开恢复、原文导出字节核对及取消通过；离线新建与并发等其余必需矩阵仍待验。
