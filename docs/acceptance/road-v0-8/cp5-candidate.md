# Road v0.8 CP5：个人测试候选与验收单

日期：2026-09-07。状态：个人测试候选已交付，**未通过 CP5 验收**。
CP0–CP4 已完成工程提交；CP4 为 `ee7d488`。本阶段不启动外部 Alpha、Python 退役或发布。

## 候选范围及隔离

- 两端使用同一源码，均为 `app.keikeu.v08candidate` 的本机签名 debug 开发候选。
  精确源码提交记录于候选旁的 `candidate-manifest.json`；构建、原生宿主回执、设备界面接受分别记录。
- Mac 候选的 Sidecar 显式接收候选 Application Support 目录，配置为 `desktop-config.json`、
  状态为 `desktop-state.json`。不继承正式桌面版的全局 Vault 选择；正式版无该参数时仍用原路径。
  本次未读取、复制、迁移或改写旧全局配置。
- iPhone 本机 Vault 与恢复区、候选的私有选择、按账号隔离的云恢复目录沿用 CP3／CP4。
  原测试数据不删除。个人测试只选新建的合成 Vault，云端只使用专用合成稿。
- Mac ZIP 与 iPhone 开发 IPA 保存在忽略的 `tests/test-vault/cp5/`，不进入 Git，不上传或发布。
  IPA 仅面向现有开发 profile 允许的设备；不是 App Store／TestFlight 包。

## 已取得的证据

| 项目 | 当前证据 | 不代表 |
| --- | --- | --- |
| CP4 在线内容往返 | Mac 创建两页 → iPhone 下载、修改、保存 → Mac 读回，见 [CP4](cp4-cloud.md) | 离线、并发、账号变化或完整 provider 验收 |
| 最终候选本机回归 | 同一 iPhone 候选重新运行隔离 seed／recover，两份宿主回执通过 | UIKit 输入、系统面板或手感 |
| 候选配置隔离修复 | Python `301 passed`；Rust `20 passed`；打包 Sidecar 独立目录启动返回 `vault_picker`；普通 Mac 候选进程实际传入独立目录 | 实体 Mac 界面回归 |
| 前端回归 | CP5 搜索修复后完整 `140 passed`，含后续页／Unicode 结果不被预览再次过滤的回归 | 原生 IME／软键盘／选区行为 |
| 五尺寸布局 | 云端合成工作面无溢出／页面异常，截图已检视 | 实体设备布局及交互接受 |
| 原生界面可操作性 | Mac 与 iPhone Mirroring 均已可操作，手机普通工作面已实际检查 | 无法据此推断手机产品故障或通过 |

## 本轮 Mac 原生检查及搜索修复

- 候选普通启动后，通过原生界面初始化新合成 Vault，创建两页、保存、切页和重开，逐页原文一致。
- 发现并修复 Library 将 Core 全文结果再次按预览过滤的问题：第二页 `Straße`／`é` 在
  `STRASSE`／组合重音 `é` 查询下原先被隐藏。展示层现在直接使用 Core 返回结果，未改 Unicode 语义。
  同一 Mac 原生合成稿复测两种查询均命中。开发原型自行维护原有演示筛选。
- 未保存输入时点击原生关窗，系统确认框出现；选择继续编辑后输入仍在，随后保存成功。
- 通过文件操作模拟外部编辑合成稿，再保存窗口旧快照，明确返回 `Paper changed externally; replacement refused`。
  磁盘文件与外部副本逐字节一致，窗口内未保存输入保持可见；保全副本与操作回执位于忽略的候选目录。
  这证明外部修改防覆盖路径，不等于完成默认外部编辑器的全部交互。
- 修复后两端重新构建并验证签名；iPhone 重新安装及 seed／recover 宿主回归通过。
  Library 在 920×680、1220×780 截图已检视，无水平溢出或页面异常。

## 手机与普通云端工作面实测

本轮两端运行 `5879815` 产品源码对应的 `search-fix/` 候选；未更改产品源码。

- 手机普通界面新增两页后，镜像自动输入曾丢字；用户手动输入 `123456手机输入检查` 完整出现在
  第二页和私有恢复记录（修订 18）。保存为 Paper v4 后，Library 重开两页及其内容一致。
  原始合成稿副本为 `phone-ui-saved.md`，SHA-256 为
  `501ac33bc59ec1e920bed73cca82425e3ab328d3c093896134462a5c4feab623`。
- 已保存 Paper 经系统分享面板保存至 On My iPhone，返回“Copy handed to the system”；
  原文草稿分享面板取消后显示“Export cancelled”，编辑原文仍在。Files 目标副本尚未读回逐字节校验，
  不能把面板成功当作全部导出验收。导出文件名为 `export-c058a11d-0d97-4aff-b0bc-b63f4faa4bad`。
- 手机选择中文后普通重启，中文设置、本机恢复列表和已保存稿保留。此时尚未完成全部输入法、
  软键盘遮挡及选区检查。镜像自动输入与真实键盘的差异原因仍未确认，保留原始回执。
- 两端通过普通界面显式选择候选专用容器 `iCloud.app.keikeu.v08probe` 的 `Documents/keikeu`。
  开始时工作面为空，本机稿件未迁移；Mac 未保存的旧合成稿在切换前另存完整副本。
- Mac 新建 `CP5 cloud UI synthetic` 两页。手机刷新显示尚未就绪及下载按钮，点击下载后再次刷新，
  显示本机可读，打开后两页原文一致。手机通过单键在第二页加入 `9` 并保存；Mac 刷新重开读回：

  ```text
  Cloud UI page one.
  云端合成第一页。

  Cloud UI page two.9
  云端合成第二页。
  ```

  Mac 文件副本保存在忽略的 `cloud-ui-readback.md`。这证明在线普通界面往返，不证明离线或并发。
- 待归类交互观察：保存后未再次输入也曾出现离开恢复草稿确认；云稿正在更新时一次刷新显示
  `Needs repair (file_or_directory_changed)`，再次刷新正常。没有据此推断损坏或擅自改写文件。
  镜像批量输入丢字也单独保留，不能据其直接归因于产品。

## 离线保存报错与连续保存修复

- 用户离线输入并保存后报告 `validation_failed`。重连前读到云恢复列表为空，镜像重连后仍可见新增正文，
  因此先将两页完整原文另存于 `offline-failure/visible-unsaved-raw.json` 及 Markdown 副本，未自动重试保存。
- 联网后 Mac 实际收到“离线手机合成修改”，该文件的更新时间为 `2026-09-07T11:00:07.179507-04:00`。
  读回副本 `offline-failure/cloud-after-reconnect.md` 的 SHA-256 为
  `f99486517ec6f7e6f1e0ea2cf2c7412121577f1bfb70bd0cbd8ead30eb939540`。
  这说明正文曾成功保存并同步；不能把随后报错解释为该次内容一定未提交。
- 定向复现确认：成功保存后，前端清空当前原文快照，紧接着再次保存会跳过恢复记录写入，
  宿主因缺少该记录返回 `validation_failed`。修复保留当前快照，让后续保存先持久化恢复记录；
  开始新操作时也清除旧成功提示，避免错误旁仍显示先前“已保存”。没有放宽宿主校验或自动重发写入。
- 回归覆盖不改正文再次保存、继续编辑后保存，以及恢复写入失败时保留输入并清除旧成功提示。
  完整前端 `141 passed`，最后定向 `8 passed`；Mac 与实体 iPhone 新候选普通界面连续两次保存均成功。
- 两端已重建、签名、解包校验；手机已安装 `save-fix/` 候选。旧候选及原始失败文件保留。
  用户确认新版离线复测正常，新增韩文测试字段成功保存。Mac 重连读回副本包含该字段，
  更新时间为 `2026-09-07T11:19:42.874248-04:00`；副本 `save-fix/offline-retest-readback.md`
  SHA-256 为 `304704a467c4f6f6d744a5ab5a0c0080c664f1c9de52fd4db442cf51ef8463a0`。
  离线编辑复测通过；离线新建仍待验，B08 不标记全部通过。

## Mac 原文恢复与导出复测

- `save-fix/` 候选新建独立两页合成草稿，输入未闭合 Tags；正式保存保持禁用。
  原生关闭时选择保留恢复草稿，重新打开候选并恢复修订 5，两页正文及未闭合 Tags 保持。
- 原生导出原文草稿到忽略的 `save-fix/export-11998f27-e96d-49ae-a78c-3841cf5ce1c8.json`，
  实际读取并逐字段核对 Tags 与两页原文，文件 SHA-256 为
  `34f0d9dec0b69bc1535c950fe612a4615601f2af4c69f136e6ab3489f6e8eae8`。
  再次导出并取消，界面显示 `Export cancelled`，修订 5、未闭合 Tags 和正文仍保留。
- 以上是 Mac 普通界面的实际恢复及原文导出证据；手机未闭合 Tags、损坏副本导出等矩阵仍待验。

## 手机重启恢复与 Mac 已保存稿导出补验

- 手机合成草稿 `K-20260907-002` 修订 24，在终止并普通重启候选前后分别复制私有恢复记录，
  逐项比较 `drafts` 完全一致；重启界面显示该修订，打开后两页与 Tags 保留。
  原始记录及回执为忽略目录 `save-fix/phone-raw-before-relaunch.json`、
  `phone-raw-after-relaunch.json` 和 `phone-raw-relaunch-receipt.json`。
- 手机实际 Tags 使用弯引号，不是 ASCII 双引号；这证明该份原文恢复，不能代替未闭合英文引号的判据。
- Mac 原生导出已保存合成 Paper 到 `save-fix/export-fbf0f379-709a-4162-bbce-3285a47ce71b.md`，
  导出文件、当前云端源文件及此前离线读回副本字节完全相同，SHA-256 为
  `304704a467c4f6f6d744a5ab5a0c0080c664f1c9de52fd4db442cf51ef8463a0`。
  原有修订 5 的独立恢复草稿仍在；见 `save-fix/mac-saved-paper-export-receipt.json`。

## 离线首次保存与真实并发冲突

- 用户确认合成稿 `K-20260907-002` 两次离线保存、一次在线保存均正常。Mac 收到的
  `save-fix/offline-first-save-readback.md` 与保存前恢复记录的两页正文和 Tags 一致，SHA-256 为
  `9a6ea2aefe99db4bf5140bf05478f3ba83afc212db1149f3b96f43d52e62423c`。
  这证明既有本机草稿的离线首次落稿；完全断网时新建并分配 code 尚未测试。
- 两端打开共同旧版本后，手机断网追加内容并保存，Mac 在线追加另一标记并保存；随后手机联网。
  Mac 普通界面执行检查并保全，私有记录实际出现 `native` 与 `active` 两份版本。
  宿主路径先完成整批原字节持久化及读回，再解决原生版本；本次操作成功返回两份可读副本。
- 两份均通过 Mac 原生导出，实际导出字节与保全记录摘要一致：
  手机版本 `b54bf0f4da85b2046f4148ecbc81913f114fbccec1517817ae8c1a2f6cfe3b9c`，
  Mac 版本 `19945bba36e95e3ae2a279a5e8fd7a34bc8fd18c83425cf8f79538269744aed3`。
  回执 `save-fix/concurrent-conflict-receipt.json`，原始保全记录
  `concurrent-mac-preserved-journal.json`；两份导出分别为
  `export-1382905d-ebaa-4011-ae70-ba2499450d7e.md` 与 `export-6f05eab3-63cb-4e56-996d-58f9786c2433.md`。
- 导出工具两次短暂超时，但文件实际生成，随后核验成功；没有因超时重复保存。
  该轮尚未执行明确恢复为活动稿，后续结果见下一节；多冲突中断、同 code 新建及 provider 改名副本仍待验。

## 恢复命令入口修复与原生复测

- 实体 Mac 点击恢复副本返回 `unsupported_method`，尚未进入恢复确认，活动稿未被改写。
  共享会话允许方法列表漏列三个已实现的恢复命令；现已补齐 inspect、promote、reconcile。
  原恢复测试改为经过实际会话入口：修复前复现相同错误，修复后 Rust 全量 `20 passed`。
- `promotion-fix/` 新候选原生恢复显示 `Recovery result: committed`，活动稿字节等于所选 Mac 副本，
  原手机版本另存为 `before-promotion`，未决恢复记录已清空。
- 在系统恢复确认打开后外部修改同一合成文件，再确认得到 `stale_snapshot`，外部字节原样保留。
  此次追加在 Tags 后的合成标记使文件出现 `invalid_tag_item`；原生导出损坏原文件，逐字节核对一致。
  重新取得确认后恢复有效版本成功，损坏原字节保存在恢复区，界面仅提供导出，不提供恢复按钮。
- 原生回执 `promotion-fix/native-promotion-receipt.json`，前后私有记录及外部副本保留在同目录。
  损坏导出位于旧默认导出目录 `save-fix/export-d63a8cc7-be25-4463-8343-80478113e835.md`，
  SHA-256 为 `0d0e5d171f940b97c2cee14c4544c6b3153a16006dd9534caa4cd56e3d9ece98`。
- 两端已重建、签名并解包核验；手机安装成功，首次自动启动因锁屏失败；随后普通启动和新版 Library 读回已验证。
  Mac 本轮已验证恢复及过期确认；多冲突中断等剩余矩阵仍未完成。

## 手机全程离线新建与 provider 自动改名副本

- 新版手机候选随后普通启动成功，Library 实际显示 Mac 恢复后的版本；先前锁屏启动阻塞已解除。
- 用户在断开 Wi-Fi 与蜂窝数据后新建并保存 `K-20260907-003`，保持离线；Mac 普通界面另行新建，
  Details 确认同编号，再保存独立内容。手机联网后 Mac 实际读到手机新稿，完成全程离线新建读回。
- provider 将 Mac 原稿自动改名为 `K-20260907-003 2.md`，内部 code 未改。首次保全因另一文件
  尚未就绪返回 `not_downloaded`；请求下载、刷新确认两份可读之后，保全成功。
- 两份保全记录均标识 `sibling`，原生导出字节与记录摘要一致：手机版本
  `fff7e0a1cd0e9a7f374c5f3395644bbcc7562d9a9b558ca73f39bdbc9c58a8f2`，Mac 改名副本
  `147b8377b5a6976ff25ebc1d98296ae7582a50899d8c78d89a6d1f54cf46a0d2`，后者与重连前原稿完全一致。
  回执 `promotion-fix/same-code-provider-receipt.json`，私有记录 `same-code-preserved-journal.json`；
  原生导出位于 `save-fix/export-513f0744-03e4-4591-9077-0fd1b3fd59c7.md` 和
  `save-fix/export-44e9f577-64a2-4ea3-862e-fec8876f27f1.md`。
- 本次覆盖同编号新建的自动改名副本分支；同编号新建产生原生版本冲突的分支仍未观察到。
  B09 的原生冲突来自已有稿件并发编辑，不能代替该分支。
- 另补验 Mac 损坏副本原生导出取消：未生成目标文件，私有草稿及全部冲突记录保持一致；
  见 `promotion-fix/damaged-export-cancel-receipt.json`。
- Mac 已保存 Paper 导出面板取消后显示 `Export cancelled`，未生成目标文件；
  前后草稿、全部冲突记录与云端合成文件摘要一致，窗口原文保持。
  见 `promotion-fix/saved-export-cancel-receipt.json`。

## 恢复操作真实进程中断

- 同一 `304ba06` Mac 候选通过 LLDB 原有调试符号，在真实专用云容器恢复操作中定点终止；
  未改源码或注入恢复记录，未更改系统调试权限。操作前已备份合成稿和完整恢复记录。
- 写入前：在 `store.rs:608` 停住，确认未决恢复记录已落盘、活动稿仍等于原字节，再终止进程。
  普通重启显示只读核对入口，恢复按钮禁用；核对返回 `not_committed`，云端字节与草稿不变。
- 写入后：在 `conflicts.rs:283` 停住，确认活动稿已等于所选副本，未决记录仍在，随后终止。
  普通重启后只读核对返回 `committed`；原活动稿已保全，所选字节、其他云稿及原文草稿保持。
- 两次核对均清除对应未决记录，冲突副本未丢失。回执为忽略目录
  `promotion-fix/interruption/before-write-interruption-receipt.json` 与
  `after-write-interruption-receipt.json`；对应中断时和核对后的私有记录均保留。
  本轮只覆盖恢复操作；普通保存结果未知及较新输入的真实中断矩阵仍待验。

## 普通保存真实进程中断与较新输入

- 同一 Mac 候选编辑既有两页合成稿，在 `store.rs:637` 停住：提交快照已持久化，文件尚未写入。
  终止并普通重启，恢复列表保留原文；打开显示保存结果未知，保存按钮禁用。
- 在未知状态追加较新输入，确认私有记录已落盘，再只读核对；返回 `not_committed`，
  新旧原文全部保留，所有云端合成文件仍逐字节等于中断前基线。
- 随后明确保存该合成输入，在 `host/mod.rs:599` 文件写入成功、响应返回之前终止。
  重启仍显示保存未知；再追加一行较新输入并核对，返回 `committed`。
  新一行只保留在恢复草稿，未写入云稿；云稿在核对前后字节一致，其他稿件及冲突副本保持。
- 两次均由候选实际写入提交记录，未注入 journal；核对后对应未决提交清除，原文仍在。
  回执、原始恢复记录及文件副本位于忽略目录 `promotion-fix/save-interruption/`，
  回执为 `before-write-receipt.json` 和 `after-write-receipt.json`。
  结合上一节，B13 的 Mac 真实保存／恢复写入前后中断及重启只读核对通过。

## 未决保存期间切换存储

- Mac 候选实际保存合成稿，在提交记录已落盘、文件写入前终止并普通重启。
  未打开恢复草稿时点击切换本机存储，确认系统面板后，宿主返回 `pending_write`。
- 切换前后存储选择文件逐字节一致，generation 未变，未决恢复记录完全一致，所有云端合成文件摘要不变。
  随后打开原存储草稿，只读核对返回 `not_committed`，原文保留且未决提交清除。
- 回执与前后记录位于忽略目录 `promotion-fix/pending-switch/`，回执为
  `pending-switch-receipt.json`。本轮覆盖未决保存切换；下载进行中切换仍待验。

## 本机桌面整理与系统交接回归

- 显式返回候选专用本机合成 Vault；云端草稿和冲突记录保持。新建独立合成稿，原生 `Cmd+S`
  保存成功，保存文件另行备份后再操作。此处本机编号与云稿编号独立，不混用证据。
- Library 将该稿移入应用废纸篓，活动路径消失、废纸篓副本逐字节等于备份；恢复后活动文件
  逐字节一致，废纸篓源路径消失。界面两次均报告一项成功、零项失败。
- 原生表单创建 `CP5 lifecycle folder`，将合成稿移动到该目录；源路径消失，目标文件字节保持。
  创建 Branch 后产生独立编号及预期名称后缀；使用正式 Python 解析器核对页内容和 Tags 一致，源稿未变。
- 默认编辑器按钮实际将所选合成稿交给 Xcode；窗口 URL 指向准确路径，正文可见，文件字节保持。
  仅关闭本次合成文件窗口，没有更改默认编辑器或其他 Xcode 工作区。
- 回执、原文及分支副本位于忽略目录 `promotion-fix/local-lifecycle/`：
  `trash-restore-receipt.json`、`move-branch-receipt.json`、`external-editor-receipt.json`。
  以上是本轮实际完成的本机路径；文件夹完整生命周期、其余本地阻塞／交互矩阵仍待验。

## 完整目录保留与系统目标错误处理

- 在已备份的本机合成目录内加入嵌套未知二进制文件，再备份完整三文件树。
  原生确认整目录移入废纸篓、随后恢复，三份文件的相对路径和摘要均与操作前一致。
  回执 `promotion-fix/local-lifecycle/folder-trash-restore-receipt.json`，完整副本保留在同级 `folder-backup/`。
- Library 对嵌套目录明确提示仅支持一级 Paper 文件夹；点击该修复项的 Finder 定位后，
  旧候选却进入 `protocol_mismatch` 阻塞页。定位发现 Rust 系统目标解析把合法错误对象按成功路径字符串解析，
  导致普通路径拒绝误升级为协议故障；打开与 Finder 定位共用此入口。
- 已改为解析并返回 `BridgeError`，没有放宽路径限制。新增回归覆盖两种系统动作拒绝后连接仍可用、
  后续合法目标仍可解析；修复前重现现场错误，修复后 Rust 全量 `21 passed`。
  两端已从 `83c4f60` 重建，现交付 `system-target-fix/`；Mac 普通启动后复测返回具体路径错误，
  Library 保持可用，随后合法合成稿仍成功交给 Xcode，文件和云端恢复记录均未变。
  原生回执为 `system-target-fix/native-system-target-receipt.json`。iPhone 新包已签名、解包校验，
  用户随后确认旧候选触屏检查一切正常；备份恢复记录后，新包安装及普通启动命令均成功。
  安装前后草稿和冲突记录一致；随后本人解锁镜像，已确认新版普通 Library 可见启动。

## 目录重命名与合并补验

- `83c4f60` Mac 候选对含未知嵌套目录的重命名明确拒绝，输入的新名称保留，目标未生成，
  原三文件树摘要不变；没有为了操作成功而放宽一级目录限制。
- 将本轮合成的嵌套目录整体移到 Vault 外保留，并核对已有备份后，原生重命名合法目录成功，
  两份 Paper 摘要不变。随后创建独立目标目录并合并，界面报告两项成功、零项失败，源目录消失，
  目标两份文件摘要与原件一致。当前合成稿位于本机 `cache/CP5 merge target/`。
- 原始未知字节仍在 `system-target-fix/retained-nested-fixture/`，不是删除或规范化。
  对应回执为同级 `folder-rename-rejection-receipt.json`、`folder-rename-success-receipt.json`、
  `folder-merge-receipt.json`。永久删除及其余本地交互仍未据此标记通过。

## 测试目录永久删除补验

- 操作范围仅为已完整备份的本机合成目录 `CP5 merge target` 及两份测试 Paper，执行前逐字节核对备份。
  整目录移入废纸篓后，永久删除系统确认明确提示不可撤销；取消后两份废纸篓文件字节保持。
- 再次明确确认后，界面报告一项成功、零项失败；目标目录从废纸篓消失，活动目录也不存在。
  Vault 外完整备份摘要重新核对通过，原有本机合成稿保留；未触及云稿或真实 Vault。
- 回执 `system-target-fix/folder-permanent-delete-receipt.json`。本轮是目录永久删除路径，
  不把它当作单稿永久删除或其余本地交互的证据。

### 单稿永久删除取消与确认（83c4f60）

- 普通 Mac 界面新建专用合成稿 `CP5 single delete fixture`，保存后在 Vault 外备份原文件。
- 单稿移入废纸篓后点击永久删除，先取消：废纸篓原文件字节与备份一致。
- 再次确认后，界面报告一项成功、零项失败；该稿在活动区及废纸篓均不存在。
  Vault 外备份摘要仍一致，原有本机合成稿保留。
- 回执与备份位于 `system-target-fix/single-delete/`：`receipt.json`、`before.json`、
  `after-cancel.json`、`original.md`。本轮补足单稿路径，不代替其余本地交互验收。

### 手机最新版恢复与导出补测（83c4f60）

- 本人解锁镜像后，普通云端 Library 可见。请求下载尚未就绪的自动改名副本后，
  经原生确认切回本机；设备选择读回为 `local`，本机 Library 和旧恢复草稿可见。
  该流程未证明 provider 实际下载进行中的时序或迟到结果隔离，B15 仍保留缺项。
- 本机草稿经分享面板“存储到文件”保存至手机本机，返回“副本已交给系统”。
  导出前后恢复 journal 字节完全一致；随后用户通过 AirDrop 交回 953 字节 JSON，全部导出字段与导出前 journal 一致；其余格式仍按 B06 单独验收。
- 用户完成新的合成稿输入后，实际私有记录为未闭合 ASCII 直引号、正文 `Cp5p1` 与 `Cp5p2`。
  备份后终止并普通重启候选，全部草稿记录一致；原生恢复界面逐页显示相同正文，
  正确提示 Tags 引号未闭合并禁用保存，旧草稿保留。B05 手机缺项已补齐。
- 回执分别为 `system-target-fix/phone-visible-and-switch-receipt.json`、
  `system-target-fix/phone-local-export/receipt.json` 和
  `system-target-fix/phone-ascii-recovery/receipt.json`，原始读回及重启日志保留于同目录。

### 本人 Mac 检查与手机导出交接

- 用户确认已完成最小 Mac 窗口、中英文输入及候选词、切页、滚动和保存检查，
  无按钮遮挡、无法滚动、输入丢失或误切页；检查稿为专用两页合成稿。
- 手机草稿 JSON 经 AirDrop 到 Mac 后实际读取，大小 953 字节，SHA-256 为
  `e4657ccac67a20a85835453adc7c1eabe9c56c543d78b150a5d5e080be923f89`。
  解析后所有导出字段与导出前恢复记录一致，两页原文完整；原件保留，副本及更新回执位于
  `system-target-fix/phone-local-export/`。此证据不代替其余导出格式。

### 手机正常 Paper 与损坏原文件导出核对

- 同一 `83c4f60` 手机候选分别执行正常 Paper 和损坏合成文件的原生导出取消／完成。
  取消后读回原文件和全部恢复草稿，均保持不变；完成后经系统“存储到文件”保存。
- 损坏合成文件包含无效 UTF-8，Library 显示 `invalid_utf8`；打开后只提供导出原文件，
  不进入编辑或覆盖路径。40 字节（含无效 UTF-8）经 AirDrop 到 Mac 后与原文件逐字节一致。
- 正常 Paper 为 362 字节，收到的目标文件与已保存 Markdown 逐字节一致。
  原件保留，验证副本及回执位于 `system-target-fix/phone-paper-export/` 和
  `system-target-fix/phone-damaged-export/`。结合已验证的草稿 JSON，B06 必需导出矩阵补齐。

### Mac 索引恢复与旧格式迁移补测（83c4f60）

- 先在普通界面保存合成检查输入，再完整备份测试 Vault。仅把可重建索引替换为损坏 JSON；
  Library 仍显示两份 Paper，并提示 Index 可能过期。点击显式重建后成功恢复有效索引，
  两份 Paper 的前后 SHA-256 完全一致。回执位于 `system-target-fix/mac-index-recovery/`。
- 新建独立旧格式合成 Vault，包含活动区及废纸篓各一份 v3 Paper；原生预检识别数量正确。
  确认后完成两份迁移，生成的完整备份与迁移前所有文件逐字节一致；活动区与废纸篓的
  总结、Tags 和未知 `source` 元数据保留，生成 Index v4，迁移报告确认备份已校验。
- 原生界面打开迁移结果，Library 显示活动稿及两页；随后通过 Vault 入口切回原合成 Vault。
  候选本地配置已核对恢复，原检查 Vault 两份 Paper 字节未变。
  回执及完整副本位于 `system-target-fix/mac-migration/`。
- 本轮证明 v3 活动区／废纸篓迁移；更早 Outline 格式见下方独立补测，其他未覆盖路径仍单独核对。

### 旧 Outline 原生迁移补测（83c4f60）

- 使用仓库明确标为合成数据的 v0.1 fixture 完整副本。原生预检列出空正文与无效状态两个阻止项，
  不提供执行迁移按钮；所有文件字节不变，也未启动备份或转换。
- 另建有效副本，保留原完整 fixture。原生预检识别两份 Cache、活动 Outline 与 Trash Outline 各一份。
  明确确认后完成 v0.1→v3；完整备份覆盖所有原文件，含两份 Outline，逐字节核对一致。
- 打开迁移结果后进入 v4 预检，再次明确确认完成 v3→v4；第二阶段完整备份与 v3 状态逐字节一致，
  最终两份 Paper 与预检映射结果逐字节一致，Index 为 v4。
- 最后通过原生 Vault 入口切回原合成检查 Vault，候选配置和两份检查稿摘要均核对保持。
  原始副本、两个阶段备份和回执位于 `system-target-fix/mac-outline-migration/`。

## 手动测试顺序

最新版镜像本人验证及可见启动已完成。后续离线步骤由本人操作手机网络，重连后代理继续核对。不要通过真实作者稿件完成下面的检查。
每项记录平台、候选摘要、合成稿标记、操作、实际结果和保全／导出文件位置；失败也保留记录。

| 编号 | 操作与通过判据 | 状态 |
| --- | --- | --- |
| B01 | Mac 普通启动到独立 Vault 选择；选择新建合成 Vault，完成创建、保存、Library、重开、原生关闭保护 | 合成 Vault 原生流程通过，关闭取消保留输入 |
| B02 | iPhone 可见启动，新增两页，分别输入正文、切页、保存、重开，页序及正文一致 | 两页合成稿保存、重开及磁盘内容核对通过 |
| B03 | 中英界面切换与重启保持；中文／英文 IME 连续输入，候选词确认不误切页或误保存 | 中文切换及重启保持、手动韩文保存读回通过；用户确认手机及 Mac 中英文输入、候选词检查正常，未出现输入丢失或误切页 |
| B04 | 最小窗口／手机软键盘下检查 safe area、按钮触控、滚动、光标与跨页选区 | 用户确认手机软键盘滚动、光标与选区检查正常（304ba06）；用户确认最小 Mac 窗口无按钮遮挡、无法滚动、输入丢失或误切页（83c4f60） |
| B05 | 未闭合 Tags 与多页原文进入后台、重启，恢复字节一致；较旧修订不能清除较新输入 | 宿主自动通过；Mac 未闭合 Tags 两页恢复通过；手机英文直引号未闭合及两页原文重启恢复通过，旧草稿保持；较新输入保留见 B13 |
| B06 | 原生导出 Paper、原文草稿及损坏副本；分别完成和取消，原稿及恢复记录保持 | 手机 Paper 导出、草稿 JSON 存储到文件及草稿取消通过，JSON 导出前后恢复记录字节一致；Mac 原文、已保存 Paper、损坏原文件导出字节核对通过，Mac 三类导出取消均通过；手机草稿 JSON 目标文件已读回并与导出前记录全字段核对一致；手机正常 Paper 与损坏副本均完成原生导出／取消，原稿及全部草稿保持，AirDrop 目标文件逐字节一致；三类导出验收补齐 |
| B07 | 两端普通工作面显式选择 iCloud；在线创建、下载、编辑与读回，核对每页实际字节 | 普通界面在线两页往返通过，见上方原文记录 |
| B08 | 合成云稿离线创建／编辑后重连，另一设备读回；不将未确认状态显示为另一设备已收到 | 离线编辑及手机全程断网新建、保存、重连后 Mac 原文读回通过 |
| B09 | 两设备基于同一旧稿并发保存；发现原生冲突，所有版本保全后才解决，逐份导出核对 | 真实原生冲突已发现，两份保全及原生导出字节核对通过 |
| B10 | 两设备同 code 新建；分别覆盖原生版本冲突和 provider 自动改名 sibling；每份字节可恢复 | 真实自动改名副本、两份保全及原生导出核对通过；同 code 新建的原生版本冲突待验 |
| B11 | 多个冲突及保全过程中终止进程；重启仍能发现未保全版本，已保全副本可校验、导出 | 两份真实原生冲突在整批提交前终止、重启仍可发现、重试保全及四份原生导出逐字节核对通过 |
| B12 | 明确恢复某有效副本前保全当前稿，外部修改令旧 inspection 失效；损坏副本只导出 | Mac 真实云稿恢复、恢复前保全、旧确认拒绝覆盖及损坏副本只导出通过 |
| B13 | 保存／恢复结果未知时重启，只读对账；不重发写入，不自动丢弃新输入 | Mac 真实保存／恢复写入前后中断、重启只读核对及保存未知时较新输入保留通过 |
| B14 | 账号／容器不可用、Drive 关闭时，显示具体状态，仍能取回并导出本机恢复；恢复连接后重新核对 | 待验；用户确认无独立测试设备／账号，日常设备账号及 Drive 设置未改动 |
| B15 | metadata-only 文件明确显示未下载，请求下载并成功打开；下载或未决保存期间尝试切换，旧请求不能污染新存储 | 普通界面下载打开及 Mac 未决保存切换被拒绝、存储与原文保持通过；手机已请求下载后切回本机，界面与设备选择一致；实际下载进行中时序及迟到结果隔离仍待验 |
| B16 | Mac 外部编辑合成稿后保存旧快照，必须拒绝覆盖并保留双方；检查既有本地功能无回归 | 旧快照拒绝覆盖、Cmd+S、单稿回收恢复、完整目录回收恢复、创建／移动／重命名／合并、Branch 及默认编辑器交接通过；目录及单稿永久删除取消／确认、损坏 Index 显式重建及 v3 活动区／废纸篓原生迁移通过；旧 Outline 预检阻止与 v0.1→v3→v4 原生迁移通过；其余本地矩阵待验 |

## 多个原生冲突保全过程中终止并重试（83c4f60）

- 在专用合成云容器中，手机离线保存 004／005，Mac 分别保存不同版本；重新联网后，
  经 `NSFileCoordinator` 只读检查确认两份文件各有一个未解决原生版本。活动稿和原生版本均先备份。
- 在读取第二个原生版本、整批恢复记录尚未提交的 `router.rs:420` 终止 Mac 进程。
  普通重启后私有 journal 与中断前逐字节一致；两个未解决版本仍各有一个，原生字节与备份一致。
- 重新点击保全，在 `router.rs:471` 观察到整批持久化已完成：副本从 7 份增至 11 份，
  原有副本及草稿不变，全部摘要正确。此处调试器随后退出并使应用继续运行，最终原生未解决版本为零；
  这次不计作“落盘后、解除前终止”证据。
- 两份稿件的活动版本及原生冲突版本共四份，均通过原生保存面板导出，目标文件与恢复记录逐字节一致。
  B11 所要求的保全过程中终止、重启发现未保全版本、重试保全及副本校验导出通过。
  证据、备份与四份导出位于 `system-target-fix/multi-conflict/`；不代替 B10 的同编号新建原生冲突。

## 回退与问题记录

- 候选使用独立标识，不替换正式桌面 app；停止候选进程可返回原 app。
  不删除候选 Vault、恢复 journal 或云冲突文件，不把旧二进制指向新私有 journal。
- 云端变化按导出的原字节副本及当前磁盘状态人工核对；不自动选赢者、合并、改 code 或重发未知写入。
- 已知工程上限：原生文件批次和单份私有 journal 串行处理，大量稿件或 provider 延迟时可能影响响应时间；
  尚无性能测量，不把它改成额外后台服务。本阶段未据此新增产品范围。
- 未解决 P0／P1 或任一必需项无证据时，停在候选待验。全部必需项通过并取得开发者接受之后，才完成 CP5。
  Road 快照、tag、push 和发布另行处理。

## 交付文件与核验

当前候选目录：`tests/test-vault/cp5/system-target-fix/`。以下两个包已实际解包并重新验证签名、bundle identifier 和可执行文件摘要。
两端源码相同；平台配置分别包含对应 Sidecar／原生薄层。最后一轮构建后未再改变产品源码。
此前 `search-fix/` iPhone 可执行文件取得 seed／recover 宿主通过回执，`save-fix/` 验证普通界面连续保存，`promotion-fix/` 验证 Mac 原生恢复；Mac 普通原生界面取得上述有限流程证据，尚非整个产品接受。

| 平台 | 安装归档 | SHA-256 |
| --- | --- | --- |
| macos | `keikeu-v08-mac-development.zip` | `0e11dd8a3c29e85483cd09f99322d429f24811c8b7fe1abe2433cd10c1698da0` |
| ios | `keikeu-v08-iphone-development.ipa` | `9e6ae414a55c8cf99d49487131a025da5f63131043dd64944481b36ff9c8165e` |

Mac ZIP 解开后可打开其中的 app；本轮 iPhone 新包已安装且普通启动命令成功，恢复记录保持；可见界面待镜像解锁核验。
安装包含现有本机开发签名，不适用于任意设备或外部分发。精确可执行文件摘要见候选清单。
本阶段既有 `compileall`、前端全量检查通过；恢复修复本轮 Rust 格式、文档与 diff 检查通过。
恢复入口修复后 Rust 全量 20 项通过；Python 和前端源码未改，本次未重复其全量回归。
旧 CP4 包保存在 `previous-cp4/`；CP5 根目录的 `7910ac3` 包保留供追溯。
`search-fix/` 保留搜索修复候选；`save-fix/` 保留连续保存修复候选；当前交付使用 `promotion-fix/` 的包及清单，勿与旧包混用。
