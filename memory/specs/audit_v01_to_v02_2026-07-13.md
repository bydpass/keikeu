# Road v0.2 开工前代码对照审计

> 日期：2026-07-13  
> 原始代码基线：`0d3d5fb` (`main`)
> 范围：只读检查当前 v0.1 实现与 Paper/Flashcard v0.2 目标的差距

## 1. 验证结果

```text
.venv/bin/python -m pytest
113 passed in 0.52s
```

结论：当前旧 Cache/Outline 代码基线稳定；本轮只修改文档，没有把 Paper/Flashcard 描述误写成已经实现。

## 2. 旧模型影响面

下列源码仍直接依赖 `Cache`、`CacheStatus`、`Outline`、`Relation`、旧读写函数或旧页面构建器：

```text
src/keikeu_core/models.py
src/keikeu_core/markdown_io.py
src/keikeu_core/indexer.py
src/keikeu_app/main.py
src/keikeu_app/pages/__init__.py
src/keikeu_app/pages/cache_page.py
src/keikeu_app/pages/library_page.py
src/keikeu_app/pages/outline_editor_page.py
```

对应测试影响面：

```text
tests/test_models.py
tests/test_markdown_io.py
tests/test_indexer.py
tests/test_app_pages.py
```

这些引用不能一次性机械删除，必须按 Planbook 的 core → migration → vault/index → UI → Outline retirement 顺序替换。

## 3. 关键差距

| 当前 v0.1 | Road v0.2 目标 | 所属 Phase |
|---|---|---|
| `Cache` + 四种创作状态 | `Paper`，无创作状态 | 1 |
| 标题 + 原始灵感 + 临时备注 | 代号 + 初稿副本 + Summary + Highlights + Tags | 1 |
| Cache/Outline 双 schema | Paper v2 单一活动 schema | 1、6 |
| Index `caches/outlines` | Index v2 `papers/errors` | 3 |
| `.trash/cache` + `.trash/outlines` | `.trash/cache` + 应用内恢复 | 3、6 |
| Cache 编辑器 | Paper 编辑器 | 4 |
| Outline 编辑器和关系 picker | 主流程不可达；Pre-Advance 后置 | 6 |
| 无 Flashcard | Summary-first Flashcard + 本地位置 | 5 |
| 文本路径 vault picker | 系统目录选择 + 普通/iCloud Drive smoke | 7 |

## 4. 高风险顺序

1. 先建立 Paper v2 I/O，不能让 GUI 自己拼 Markdown。
2. 在删除旧 Cache reader 前完成迁移器与失败注入测试。
3. 先验证全部 Cache 转换，再删除活动 Outline。
4. Outline 退休必须发生在迁移能力稳定之后。
5. iCloud Drive 等目录只按普通文件路径处理；不得顺手引入 provider API。

## 5. 开工门槛

进入 Phase 1 前仍需要开发者完成一次文档审阅，确认：

- Paper v2 Markdown 格式
- 初稿副本语义
- 旧 Cache 字段映射
- 旧 Outline 仅保留在完整备份中的销毁语义
- Road v0.2 只验收 macOS

## 6. Phase 0 执行记录（2026-07-14）

- 开发者已明确授权开始执行 Road v0.2；`spec` 与 `planbook` 的状态已同步为执行中。
- 复核当前 `main` 为 `63d3cbd`（仅 GPL 许可证与 README 变更）；v0.1 功能基线仍为本文件记录的 `0d3d5fb`。
- `.venv/bin/python -m pytest` 在当前 HEAD 上通过 `113 passed`。
- 以 `CacheStatus`、`Cache`、`Outline`、`Relation`、旧 I/O 与 index API 为词元扫描，`src/` 与 `tests/` 共有 512 处匹配。直接影响的实现仍为 core 的 `models`、`markdown_io`、`indexer`、`vault`，以及 app 的主导航、Cache、Library、Outline 页面；测试影响面为 `test_models`、`test_markdown_io`、`test_indexer`、`test_vault`、`test_app_pages`。
- 冻结合成迁移夹具 `tests/fixtures/v01-vault/`：有效 Cache、可解析但 Summary 不可用的空原始灵感 Cache、可解析的空可选字段 Cache、损坏 Cache、有效活动 Outline 与回收站 Outline。`tests/test_v01_fixture.py` 锁定这些样本的 v0.1 解析行为，供 Phase 2 直接复用。

剩余风险：该夹具证明迁移逻辑的边界，但不替代后续对用户真实 vault 副本的手工演练；真实迁移仍必须在 Phase 2 完成并通过后才可进行。

## 7. Phase 1 执行记录（2026-07-14）

改动文件：

- `src/keikeu_core/models.py`
- `src/keikeu_core/markdown_io.py`
- `tests/test_models.py`
- `tests/test_markdown_io.py`

交付行为：

- 新增纯 Python `Paper` v2 模型：规范代号、非空 Summary、顺序 Highlights、去空条目、Tags 清理去重与 `legacy_title` 保全。
- 新增 Paper v2 Markdown 读写：`type: paper`、`schema_version: 2`、初稿副本、Summary、Highlights、Tags、日期与可保留的未知 frontmatter。
- 首次写入在原子新建成功后冻结初稿副本；普通更新不能改写初稿副本、`legacy_title` 或 Paper 代号。
- 新增同目录临时文件 + 原子替换更新；新建和显式 rename 都拒绝覆盖已有代号文件。

检查结果：

```text
.venv/bin/python -m pytest
135 passed in 0.44s

rg -n '^(from|import) flet' src/keikeu_core
# no matches

git diff --check
# clean
```

剩余风险：旧 Cache/Outline UI、v1 vault/index 和实际迁移器仍未改动。Paper v2 已可由 core 独立读写，但在 Phase 2 前不能对真实旧 vault 执行任何迁移或删除。

## 8. Phase 2 执行记录（2026-07-14）

改动文件：

- `src/keikeu_core/migration_v01.py`
- `tests/test_migration_v01.py`

交付行为：

- 只读识别 v0.1 vault，并在写入前列出所有损坏 Cache、空原始灵感和不支持的 Cache 目录项。
- 对完整 vault 建立 vault 外字节复制备份；迁移只接受通过预检的来源。
- 在 vault 外 staging 副本中转换 Cache，逐个读回验证 Paper 的初稿副本、Summary、Highlights、日期与 `legacy_title`。
- 同时转换回收站 Cache，避免 v2 vault 留下可被后续恢复路径重新带回的 v0.1 Markdown。
- staging 完成 v2 index 与迁移报告后，以完整 vault 目录切换替换活动 vault；旧 Outline 与回收站 Outline 只保留在备份中，且从不解析或转换。
- 受控失败会恢复原 vault；迁移报告记录 Cache 映射、旧状态、Outline 关联与被移除的 Outline。

检查结果：

```text
.venv/bin/python -m pytest
145 passed in 0.53s

rg -n '^(from|import) flet' src/keikeu_core
# no matches

git diff --check
# clean
```

覆盖的失败路径：单 Cache 预检失败、备份复制失败、备份后/切换前失败、原 vault 已改名后的切换失败、同日多 Cache 代号，以及回收站 Cache 转换。

剩余风险：没有也不得在本 Phase 对真实用户 vault 执行迁移。Phase 6 仍需提供用户确认、迁移说明与结果展示；Phase 3 将把这里写入的最小 v2 index 收敛为正式可恢复 index 实现。进程被强制终止在两次目录 rename 之间时仍依赖同级 rollback 目录人工恢复，不能由本轮受控失败测试替代。

## 9. Phase 3 执行记录（2026-07-14）

改动文件：

- `src/keikeu_core/vault.py`
- `src/keikeu_core/indexer.py`
- `src/keikeu_core/markdown_io.py`
- `tests/test_vault.py`
- `tests/test_indexer.py`
- `tests/test_app_pages.py`（仅 v0.1 GUI 合同的显式隔离标记）

交付行为：

- 新 vault 只创建 `cache/`、`.trash/cache/` 和可丢弃的 v2 index；不再创建 Outline 目录，`is_vault` 只要求 `cache/` 与 index。
- Index v2 只投影活动 Paper 的代号、路径、Summary、Tags 和日期；单文件解析失败或文件名/代号不一致时记录相对路径和原因，其他 Paper 仍可索引。
- 缺失或无效 index 可由 Markdown 重建；显式 rebuild 会移除外部删除的条目，且永不修改 Paper 或回收站字节。
- 软删除只接受活动 `cache/*.md`，移动后字节不变且不覆盖同名回收站文件；恢复列表返回排序的相对路径。
- 无冲突恢复直接移回原始字节；活动代号冲突时必须取消或提供未占用的新代号，只有后者经 Markdown I/O 原子创建并保留初稿副本、当前 Summary 和未知 frontmatter。
- v0.1 Cache/Outline GUI 合同已显式隔离，两个旧 index import shim 不读取旧资产；Phase 4 必须替换 UI 后才能移除该隔离。

检查结果：

```text
.venv/bin/python -m pytest tests/test_vault.py tests/test_indexer.py
18 passed in 0.15s

.venv/bin/python -m pytest
103 passed, 35 skipped in 0.51s

rg -n '^(from|import) flet' src/keikeu_core
# no matches

git diff --check
# clean
```

剩余风险：现有 Flet 页面仍是被隔离的 v0.1 Cache/Outline 界面，不能作为 v2 可用性证据；Phase 4 必须完成 Paper/Library UI 并恢复相应 UI 测试。真实用户 vault 仍未执行迁移，迁移确认与结果展示留待 Phase 6；Phase 7 仍需 macOS 与 iCloud Drive smoke test。

## 10. Phase 4 执行记录（2026-07-14）

改动文件：

- `src/keikeu_app/main.py`
- `src/keikeu_app/pages/paper_page.py`
- `src/keikeu_app/pages/library_page.py`
- `src/keikeu_app/pages/__init__.py`
- `tests/test_app_pages.py`

交付行为：

- 主导航已改为“纸片、Flashcard、本地文件库”；旧 Cache/Outline 页面不再由正常导航导入或可达。Flashcard 路由只显示明确的下一 Phase 占位，不提前保存卡片状态或提供卡内编辑。
- Paper 页面支持新建、重开、当前 Summary 编辑、只读初稿副本、Highlights 增删/排序/编辑、Tags 自由逗号输入与已有 Tag 提示。
- 空 Summary 在 UI 层给出可解释错误且不写文件；首次保存后 Paper 代号只读，重命名必须经单独的新代号输入和 core `rename_paper`。
- Library 单一搜索框匹配代号、Summary 和 Tags；每个活动 Paper 可编辑、打开 Flashcard 占位、交给系统默认程序打开、Finder 定位或移入回收站。
- Library 显示 index 隔离的损坏 Paper，并显示回收站、恢复和代号冲突时的新代号输入；所有文件操作仍委托 core。

检查结果：

```text
.venv/bin/python -m pytest tests/test_app_pages.py
11 passed in 0.22s

.venv/bin/python -m pytest
114 passed in 0.52s

rg -n '^(from|import) flet' src/keikeu_core
# no matches

git diff --check
# clean

.venv/bin/flet run src/keikeu_app/main.py
# 启动命令无 CLI 错误返回；自动化环境未替代人工可见交互检查
```

剩余风险：Flashcard 仍未实现，设备本地位置也尚不存在（Phase 5）。旧 Outline 源码仍作为不可达 legacy 保留至 Phase 6；迁移确认/报告 UI 也仍待 Phase 6。Phase 7 必须由人工验证普通目录和 iCloud Drive 中的完整可见交互与文件行为。
